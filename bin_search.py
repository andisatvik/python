# Binary search to check if an element is present in a list

import random

num_list = random.sample(range(20),7)

num = int(input("Enter the number you are searching for: "))

def num_pres(num_list, num):

    #sorting to prepare for binary search
    num_list.sort()
    if (num < num_list[int(len(num_list)/2)]):
        num_list = 

    if :
        print(str(num)+" present in list")
    else:
        print(str(num)+" present in list")
